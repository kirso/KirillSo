---
import { type CollectionEntry, render } from "astro:content";
import type { HTMLTag, Polymorphic } from "astro/types";
import FormattedDate from "@/components/FormattedDate.astro";

type Props<Tag extends HTMLTag> = Polymorphic<{ as: Tag }> & {
	note: CollectionEntry<"note">;
	isPreview?: boolean | undefined;
};

const { as: Tag = "div", note, isPreview = false } = Astro.props;
const { Content } = await render(note);
---

<article
  class:list={[
    isPreview && "border-l-2 border-border pl-4 py-2",
  ]}
  data-pagefind-body={isPreview ? false : true}>
  <Tag class:list={[isPreview ? "text-base font-medium" : "page-title mb-2"]}>
    {
      isPreview ? (
        <a class="link" href={`/notes/${note.id}/`}>
          {note.data.title}
        </a>
      ) : (
        <>{note.data.title}</>
      )
    }
  </Tag>
  <FormattedDate
    class="text-sm text-text-tertiary"
    date={note.data.publishDate}
  />
  <div
    class="prose prose-sm mt-3 max-w-none [&>p:last-of-type]:mb-0"
    class:list={{ "line-clamp-3": isPreview }}>
    <Content />
  </div>
</article>
