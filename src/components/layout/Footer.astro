---
import { menuLinks, siteConfig } from "@/site.config";

const year = new Date().getFullYear();
---

<!-- End mark — consistent page termination -->
<div class="end-mark"></div>

<footer
  class="mt-8 flex flex-col items-center justify-center gap-y-2 border-t border-border pt-8 pb-4 text-center text-text-secondary sm:flex-row sm:justify-between"
  style="font-size: var(--text-micro);"
>
  <div class="me-0 sm:me-4">
    {siteConfig.author} &copy; {year}
  </div>
  <nav
    aria-labelledby="footer_links"
    class="flex gap-x-4"
  >
    <p id="footer_links" class="sr-only">More on this site</p>
    {
      menuLinks.map((link) => (
        <a
          class="link-muted"
          href={link.path}
        >
          {link.title}
        </a>
      ))
    }
    <a class="link-muted" href="/tags/">Tags</a>
    <a class="link-muted" href="/rss.xml">RSS</a>
    <a class="link-muted" href="/privacy/">Privacy</a>
  </nav>
</footer>
